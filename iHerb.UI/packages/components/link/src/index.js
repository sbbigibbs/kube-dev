import React from "react"
import { Text, Stylesheet, View } from 'react-primitives';

export default ({text, url}) => {
    <View className="a">
        <Text>
            {text}
        </Text>
    </View>
}
