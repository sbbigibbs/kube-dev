import { workflowCart } from "iherb-redux"
import co from "co"

var actionTypes = workflowCart.actionTypes
var actions = workflowCart.actions

export const GetRecommendationsHandler = props => store => next => action => co(function*(){
})