
const cart = state => state.cart

const products = state => state.products

const shippingMethods = state => state.shippingMethods

const workflowCart = state => state.workflowCart

const config = state => state.config

export default {
    cart,
    products,
    shippingMethods,
    workflowCart,
    config
}